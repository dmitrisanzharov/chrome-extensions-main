(() => {
    console.log('content.js loaded');

    // const banner = document.createElement('div');
    // banner.style.cssText =
    //     'position:fixed;top:0;left:0;width:100%;background:lime;color:black;font-size:20px;z-index:999999;text-align:center;padding:8px;';

    // const img = document.createElement('img');
    // img.src = chrome.runtime.getURL('assets/575927.png');
    // img.style.cssText = 'height:40px;vertical-align:middle;margin-right:8px;';
    // banner.appendChild(img);

    // const text = document.createTextNode('Extension content script is running!');
    // banner.appendChild(text);

    // document.body.appendChild(banner);


})();
